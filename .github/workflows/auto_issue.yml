name: auto issue workflow

on:
  push:
  workflow_dispatch:

jobs:
  build:
    name: auto issue
    runs-on: ubuntu-latest
    steps:
      - name: create issue
        run: |
          gh auth login --with-token ${{ secrets.GITHUB_TOKEN }}
          gh issue create --title "I found a bug"
